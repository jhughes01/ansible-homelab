---
- name: NGINX Docs VM
  hosts: localhost
  user: ansible
  become: true

- name: Install ansible role dependencies via Galaxy
  include_playbook: playbooks/install-roles.yml

  roles:
    - { role: common }
